---
description: "Build local OCI image (runs `just oci`). Args: `[executor] [tag]`, defaults `podman local`."
allowed-tools: Bash(just oci:*)
---

Run `just oci $ARGUMENTS` and report the image tag produced.

!just oci $ARGUMENTS

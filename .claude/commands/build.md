---
description: Build dist/imapsync-go for the host platform (runs `just build`).
allowed-tools: Bash(just build:*), Bash(just build)
---

Run `just build`. Report the path of the produced binary and its size.

!just build

---
description: Run golangci-lint on the whole module (runs `just lint`).
allowed-tools: Bash(just lint:*), Bash(just lint)
---

Run `just lint` and surface any issue grouped by file. If clean, say so in one line.

!just lint

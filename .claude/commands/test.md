---
description: Run the full test suite (runs `just test`).
allowed-tools: Bash(just test:*), Bash(just test)
---

Run `just test` and report pass/fail with package-level counts. Surface any failure with the assertion that failed.

!just test

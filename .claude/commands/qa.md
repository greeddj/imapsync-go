---
description: Hand a test/coverage audit brief to the `tester` agent.
argument-hint: <focus packages and invariants; e.g. "internal/client reconnect + selectIfNeeded cache invalidation">
---

Invoke the `tester` subagent. The prompt below is the focus brief.

Focus:

$ARGUMENTS

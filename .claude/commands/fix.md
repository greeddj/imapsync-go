---
description: Auto-fix go fix + fieldalignment (runs `just fix`).
allowed-tools: Bash(just fix:*), Bash(just fix)
---

Run `just fix`. Then `git diff` to show what was changed. Do not commit.

!just fix

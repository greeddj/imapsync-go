---
description: Run vet + staticcheck + govulncheck + fieldalignment (runs `just check`).
allowed-tools: Bash(just check:*), Bash(just check)
---

Run `just check` and report findings grouped by tool. If clean, say so in one line.

!just check

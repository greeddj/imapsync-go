---
description: Cross-build for linux/amd64 (runs `just build_linux`).
allowed-tools: Bash(just build_linux:*), Bash(just build_linux)
---

Run `just build_linux`. Report the path of the produced binary.

!just build_linux
